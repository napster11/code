#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
void mid(vector<int> v)
{
	int temp = v.size();
	if(temp%2 != 0) {
		cout << v[n/2-1] << endl;
		v.erase(v.begin()+n)
int main()
{
	while(!cin.eof()) {
		vector<int> v;
		int a;
		for(int i = 0;;i++) {
			cin >> a;
			if(a == 0) {
				break;
			}else if(a == -1) {
				mid(v);
			}else {
				v.push_back(a);
			}
		}
		v.clear();
	}
	return 0;
}










